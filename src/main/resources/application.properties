spring.application.name=bingetogether-backend
server.port=${PORT:8080}
# 🌐 Connect directly to your live AWS PostgreSQL RDS database instance
spring.datasource.url=jdbc:postgresql://bingetogether.cmb6cm6gm086.us-east-1.rds.amazonaws.com:5432/bingeTogether
spring.datasource.username=Sakshi
spring.datasource.password=voidSakshi154
spring.datasource.driver-class-name=org.postgresql.Driver

# 🔄 PostgreSQL Hibernate Dialect & Configurations
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# 🔑 Hardcoded development signature string to stop restarts breaking current sessions
jwt.secret=mySuperSecretCustomSecureSigningKeyForBingeTogetherProject2026!

# ⏳ Connection Pool Management to prevent future crashes on container restarts
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=20000