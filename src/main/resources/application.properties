spring.application.name=bingetogether-backend
server.port=${PORT:8080}

# 🌐 FIXED: Pulls the precise public proxy endpoint directly from Railway's environment mapping
spring.datasource.url=${DATABASE_PUBLIC_URL}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

jwt.secret=mySuperSecretCustomSecureSigningKeyForBingeTogetherProject2026!